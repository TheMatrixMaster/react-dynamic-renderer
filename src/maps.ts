import * as T from './types';

export const StyleMap = {} as T.StyleMap;
export const ActionMap = {} as T.ActionMap;
export const VariableMap = {} as T.VariableMap;
export const ComponentMap = {} as T.ComponentMap;
